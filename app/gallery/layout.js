export const metadata = {
  title: 'Gallery — Perinba Vilas',
  description: 'Family gallery moments from Perinba Vilas — every photo shown in full.',
};

export default function GalleryLayout({ children }) {
  return children;
}
